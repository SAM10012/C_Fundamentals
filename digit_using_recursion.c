#include<stdio.h>
int count(int arr[15]);
void main()
{
	int z,num[15];
	printf("Enter the number:\n");
	scanf("%d",&num);
	z=count(num);
	printf("The no. of digits in the given no. is %d\n",z);
}
int count(int arr[15])
{
	int i=0,sum=0;
	if((*(arr+i)>48)&&(*(arr+i)<57))
	{
		sum=(sum+1);
		
	int	count(int arr[15]);
	i++;
	return sum;
}
}
	
	
	

