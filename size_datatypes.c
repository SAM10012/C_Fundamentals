#include<stdio.h>
#include<conio.h>
void main()
{
	int a;
	char b;
	float c;
	double d;
	
	printf("The size of a(int type) is %d bytes\n",sizeof(a));
	printf("The size of b(char type) is %d byte\n",sizeof(b));
	printf("The size of c(float type) is %d bytes\n",sizeof(c));
	printf("The size of d(double type) is %d bytes\n",sizeof(d));
	
	getch();
}
	
